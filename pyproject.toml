[project]
name = "karanjakhar-site"
version = "0.1.0"
description = "Custom static site generator for karanjakhar.net"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1",
    "markdown>=3.5",
    "Pygments>=2.17",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
